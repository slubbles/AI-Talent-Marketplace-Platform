{
  "name": "@atm/db",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo \"Database package build is handled by Prisma\"",
    "db:generate": "prisma generate",
    "dev": "echo \"Use prisma migrate commands in later sessions\"",
    "lint": "prisma validate",
    "typecheck": "prisma validate"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1"
  },
  "devDependencies": {
    "prisma": "^6.4.1"
  }
}
